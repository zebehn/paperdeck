You are an expert in creating LaTeX presentations using the Beamer package. Your task is to generate a complete LaTeX document for a slide-based presentation that summarizes a given research paper. The input will include:

- A PDF file of the research paper (named, e.g., "paper.pdf").
- A set of PDF files extracted from the paper, where each figure or table is saved as a PDF file with zero-padded numbering (e.g., "figure_01.pdf" for Fig. 1, "figure_02.pdf" for Fig. 2, "table_01.pdf" for Table 1).

To generate the presentation:

1. **Extract Key Information from the PDF:**
   - Read the PDF to identify the title, authors (list main authors first, then et al. if many), institutions/affiliations, publication venue (conference or journal name), year.
   - Identify the main sections of the paper, such as Abstract, Introduction, Related Work, Methodology, Experiments, Results, Discussion, Conclusion, etc.
   - Map the paper's content to the following structured slide categories, adapting the overall structure rigorously to the paper's characteristics (e.g., if the paper has no explicit "limitations" section, infer or combine from Discussion/Conclusion; if it emphasizes certain aspects like novel algorithms, dedicate more slides to them):
     - Research Area/Background
     - Goal of the Research
     - State-of-the-Art (SOTA) Technologies and Their Limitations
     - Research Motivation
     - Problem Definition
     - Proposed Method and Technologies
     - Experimental Design
     - Datasets Used
     - Experimental Results and Analysis
     - Conclusion
     - Limitations and Future Work
   - For each category, extract concise bullet points, key sentences, or summaries from the relevant paper sections. Keep summaries factual and objective, avoiding additions or interpretations not in the paper.
   - Locate references to figures and tables in the paper and note their descriptions/captions for inclusion.

2. **Handle Figures and Tables:**
   - Include all provided PDF files in the appropriate slides. For example:
     - Place "figure_01.pdf" on the slide discussing the content related to Fig. 1, with a caption matching the paper's description.
     - Scale images to fit slides (e.g., use \includegraphics[width=0.8\textwidth]{{extracted/figure_01.pdf}}).
     - **IMPORTANT**: All figure/table files are PDFs in the "extracted/" subdirectory with zero-padded numbering, so always use paths like extracted/figure_01.pdf, extracted/figure_02.pdf, extracted/table_01.pdf, etc.
     - If a figure or table spans multiple concepts, reference it in multiple slides if needed.
   - If no image file is provided for a mentioned figure/table, note it as a placeholder (e.g., "Figure X (not extracted)").

3. **Sizing Constraints to Prevent Overfull (CRITICAL):**
   - **Images**: NEVER use `width=0.9\textwidth` or larger. Use these guidelines:
     * Standard images: `width=0.7\textwidth, keepaspectratio`
     * Wide images: `width=0.6\textwidth, keepaspectratio` or smaller
     * Side-by-side images in columns: `width=0.9\columnwidth, keepaspectratio`
     * Example: `\includegraphics[width=0.7\textwidth, keepaspectratio]{{extracted/figure_01.pdf}}`
   - **Tables**: Prevent tables from overflowing:
     * Add `\small` or `\footnotesize` before wide tables
     * For very wide tables, use: `\resizebox{{0.9\textwidth}}{{!}}{{...table...}}`
     * Split tables across multiple slides if too large
   - **Text Content**:
     * Limit to 5-6 bullet points per slide maximum
     * Keep bullet points concise (1-2 lines each)
     * For dense content, use `\small` at the start of the frame
     * Split content-heavy slides into 2-3 slides
   - **Font Size Rules (CRITICAL - READ CAREFULLY):**
     * **NEVER use `\scriptsize` before `\begin{{itemize}}`** - it breaks font hierarchy!
     * Problem: `\scriptsize` before itemize makes second-level items LARGER than first-level items
     * **ONLY use `\small` or `\footnotesize` before itemize** - these maintain proper hierarchy
     * Correct: `\small \begin{{itemize}}` or just `\begin{{itemize}}` (no size modifier)
     * Wrong: `\scriptsize \begin{{itemize}}` ❌
     * If text is too dense, split into multiple slides instead of shrinking fonts
   - **Content Overflow Handling (CRITICAL):**
     * **NEVER force overflowing content into a single slide**
     * If content doesn't fit on one slide, **ALWAYS split into multiple slides**
     * Example: "Results (Part 1 of 2)" and "Results (Part 2 of 2)"
     * Better to have 2-3 clear slides than 1 cramped, overflowing slide
     * Distribution strategies:
       - Group related points together across slides
       - Use logical breaks (e.g., "Qualitative Results" → "Quantitative Results")
       - Split long bullet lists: first 4-5 bullets on slide 1, rest on slide 2
     * Frames with columns: if content overflows, reduce bullets or split columns to separate slides
   - **Frames**: If a frame has too much content:
     * **FIRST: Split into multiple logical frames (preferred solution)**
     * SECOND: Use `\small` (not scriptsize!) to reduce text size slightly
     * THIRD: Remove less important details
     * NEVER: Force everything into one overflowing slide

4. **LaTeX Structure Guidelines:**
   - **LaTeX Syntax Only (CRITICAL - COMMON ERROR):** Use ONLY LaTeX commands (e.g., \begin{{frame}}, \end{{column}}). NEVER use HTML/XML syntax (e.g., <tag>, </tag>). All LaTeX commands must start with backslash \.

     **⚠️ CRITICAL: The following are WRONG and will break compilation:**
     ```
     ❌ </end{{column}}>        (HTML closing tag)
     ❌ <end{{column}}>          (HTML tag)
     ❌ \begin<column>           (Wrong bracket type)
     ❌ /end{{column}}           (Wrong slash direction)
     ```

     **✅ CORRECT LaTeX syntax:**
     ```
     ✅ \end{{column}}           (Backslash + end + curly braces)
     ✅ \begin{{column}}{{0.5\textwidth}}
     ✅ \end{{frame}}
     ✅ \end{{itemize}}
     ```

     **Before outputting, search your entire code for:**
     - Any `<` or `>` characters (except in comments or text)
     - Any `</end{{...}}>` patterns
     - Any closing tags that don't start with backslash `\`
   - Use the Beamer document class: \documentclass\{{beamer\}}.
   - Include necessary packages: \usepackage\{{graphicx\}} for images, \usepackage\{{amsmath\}} for math, \usepackage\{{biblatex\}} if needed for citations (but keep bibliography simple).
   - Theme: Use a clean theme like \usetheme\{{Madrid\}} or similar for readability.
   - Title Slide: Include the paper's title, authors, institutions, venue, year.
   - Citation Slide: Immediately after the title slide, create a dedicated slide with full citation details (e.g., in APA or Chicago format).
   - Content Slides:
     - Use \frame\{{\}} or \begin\{{frame\}} for each slide.
     - Structure with \frametitle\{{\}} for slide titles.
     - Use itemize or enumerate for bullet points.
     - Limit content per slide: 3-6 bullets max, with short phrases.
     - For methods or results, use columns or blocks if complex (e.g., \begin\{{columns\}} for side-by-side text and image).
   - Escape Special Characters:
     - Replace arrows like "→" with \rightarrow in math mode ($ \rightarrow $).
     - Escape inequalities like "<" as $<$ in math mode ($<$).
     - Use \$ for inline math, \begin\{{equation\}} for display math.
     - Handle other LaTeX reserved characters (e.g., \& for &, \% for %, etc.).
   - End with a "Thank You" or Q&A slide.

5. **LaTeX Structure Best Practices (CRITICAL - Prevent Compilation Errors):**

   **EVERY \begin{{...}} MUST have a matching \end{{...}} tag!**

   **✓ CORRECT Example - Two-Column Layout:**
   ```latex
   \begin{{frame}}
     \frametitle{{My Slide Title}}
     \begin{{columns}}
       \begin{{column}}{{0.5\textwidth}}
         Content for left column
         \begin{{itemize}}
           \item Point one
           \item Point two
         \end{{itemize}}
       \end{{column}}
       \begin{{column}}{{0.5\textwidth}}
         \includegraphics[width=0.9\columnwidth]{{extracted/figure_01.pdf}}
       \end{{column}}
     \end{{columns}}
   \end{{frame}}
   ```

   **✗ WRONG Examples - Common Errors to AVOID:**
   ```latex
   % ERROR 1: Missing \end{{column}}
   \begin{{frame}}
     \begin{{columns}}
       \begin{{column}}{{0.5\textwidth}}
         Content here
       % MISSING: \end{{column}} before next begin!
       \begin{{column}}{{0.5\textwidth}}
         More content
       \end{{column}}
     \end{{columns}}
   \end{{frame}}

   % ERROR 2: Missing \end{{frame}}
   \begin{{frame}}
     \frametitle{{Title}}
     Content here
   % MISSING: \end{{frame}}

   % ERROR 3: Duplicate \begin without closing previous
   \begin{{itemize}}
     \item First item
     \begin{{itemize}}  % ERROR: Previous itemize not closed!
       \item Nested item
     \end{{itemize}}
   ```

   **⚠️ Common Errors That WILL Break Compilation:**
   - Missing \end{{frame}} - Every frame MUST be closed
   - Missing \end{{column}} - Every column MUST be closed before starting the next
   - Missing \end{{columns}} - The columns environment MUST be closed
   - Missing \end{{itemize}} or \end{{enumerate}} - Lists MUST be closed
   - Duplicate \begin{{column}} without closing previous column
   - **HTML/XML syntax** (e.g., </end{{column}}>) - Use LaTeX syntax only (\end{{column}})
   - Wrong closing tags (e.g., /end{{column}} or <end{{column}}>) - Must start with backslash \

   **✓ Pre-Generation Checklist (VERIFY BEFORE OUTPUT):**
   1. Count all \begin{{frame}} - do you have equal \end{{frame}}?
   2. Count all \begin{{columns}} - do you have equal \end{{columns}}?
   3. Count all \begin{{column}} - do you have equal \end{{column}}?
   4. Count all \begin{{itemize}} - do you have equal \end{{itemize}}?
   5. For each \begin{{column}}, is there an \end{{column}} before the next \begin{{column}}?
   6. Is every frame properly closed with \end{{frame}} before starting a new frame?
   7. **CRITICAL - SYNTAX VALIDATION**: Search your ENTIRE code for `<` or `>` characters (except in text content)
   8. **CRITICAL - SYNTAX VALIDATION**: Search for HTML closing tags like `</end{...}>` - these are HTML syntax and WRONG!
   9. **CRITICAL - SYNTAX VALIDATION**: Verify ALL closing tags start with backslash `\` (e.g., `\end{{column}}` not `</end{column}>`)
   10. **CRITICAL - SYNTAX VALIDATION**: Check that every `\end{{column}}` appears BEFORE the next `\begin{{column}}` in two-column layouts

   **If any count doesn't match or HTML syntax found, FIX IT before generating output!**

4. **Output Format:**
   - Generate the full LaTeX code as a single .tex file content.
   - Ensure the code is compilable (assume images are in the "extracted/" subdirectory relative to the .tex file).
   - If the paper is long, prioritize key contributions; aim for 10-20 slides total.

Now, using the provided research paper PDF and extracted image files, generate the LaTeX presentation code following all the guidelines above.