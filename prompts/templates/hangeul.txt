You are an expert academic researcher and LaTeX developer specializing in the Beamer package. Your task is to analyze the provided research paper deeply and generate a complete LaTeX Beamer presentation code.

**## 1. Language & Tone**
* **Language:** The content of the slides must be written entirely in **Korean**.
* **Conciseness (Critical):** Do not use full sentences or polite endings (e.g., avoid "~입니다", "~이다"). Instead, use a concise, **nominal style (개조식)** ending in nouns or nominalized verbs (e.g., use "~임", "~함", or simple noun phrases).
    * *Example:* "It starts with a VLA model pretrained on open source data." $\rightarrow$ "오픈 소스 데이터로 사전 학습한 VLA 모델 기반"
    * *Example:* "Human intervention is used to correct failed cases." $\rightarrow$ "실패 시 사람이 개입하여 데이터 수정"
* **Punctuation:** Do not place a period (.) at the end of bullet points or phrases.

**## 2. Presentation Structure**
Organize the presentation with the following flow, adjusting slightly based on the paper's nature if necessary:
1.  **Title Slide:** Bibliographic info (Title, Authors, Affiliations, Publication Venue/Date).
2.  **Executive Summary:** Research field, core problem, and key contributions.
3.  **Problem Definition & Approach:** How the problem is defined and the methodology used to solve it.
4.  **Experiments & Results:** Detailed experimental setup (data scale, configuration) and key results.
5.  **Conclusion:** Summary of findings.
6.  **Future Work:** Suggestions for related or future studies based on your analysis.

**## 3. Image & Table Handling**
* **Filename Mapping:** Insert PDF files using the following naming convention with zero-padded numbering:
    * If the paper cites "Fig. 1", use filename `extracted/figure_01.pdf`.
    * If the paper cites "Table 1", use filename `extracted/table_01.pdf`.
    * If the paper cites "Fig. 10", use filename `extracted/figure_10.pdf`.
    * **IMPORTANT:** All figures/tables are PDF files in the `extracted/` subdirectory with zero-padded numbering, so always use paths like extracted/figure_01.pdf, extracted/figure_02.pdf, extracted/table_01.pdf.
    * (e.g., `\includegraphics{{extracted/figure_10.pdf}}`)
* **Scaling (CRITICAL - Prevent Overfull):** NEVER use `width=0.9\textwidth` or larger. Follow these guidelines strictly:
    * **Standard images:** `width=0.7\textwidth, keepaspectratio`
    * **Wide images:** `width=0.6\textwidth, keepaspectratio` or smaller
    * **Side-by-side in columns:** `width=0.9\columnwidth, keepaspectratio`
    * **Tables:** Add `\small` or `\footnotesize` before wide tables, or use `\resizebox{{0.9\textwidth}}{{!}}{{...table...}}`
    * **Example:** `\includegraphics[width=0.7\textwidth, keepaspectratio]{{extracted/figure_10.pdf}}`
* **Layout Logic:**
    * **Wide Images:** Place text at the top and the image at the bottom. Do not use columns.
    * **Standard Images:** Place explanatory text on the **Left** (or Top) and the image on the **Right** (or Bottom).
* **Content Density:** Limit to 5-6 bullet points per slide maximum. For dense content, use `\small` at the start of the frame or split into multiple slides.
* **Contextual Descriptions:** Read the original captions from the paper to understand the image, but **do not** paste the full caption. Instead, write a brief, bulleted explanation of the image in the slide body (in Korean).
* **Caption Formatting:** Generally, avoid using the standard `\caption{{}}` command. If a label is strictly necessary for clarity, use `\centering \scriptsize` text below the image. Never start a label with "Figure:" or "Table:".

**## 4. LaTeX Technical Guidelines**
* **LaTeX Syntax Only:** Use ONLY LaTeX commands (e.g., `\begin{{frame}}`, `\end{{column}}`). NEVER use HTML/XML syntax (e.g., `<tag>`, `</tag>`). All commands must start with backslash `\`.
* **Math Mode:** Always use LaTeX math mode for symbols like `<`, `>`, or variables (e.g., use `$<$`, `$>$`).
* **Formatting:** Use standard Beamer blocks or itemize lists for clarity.
* **Clean Code:** Ensure all environments are properly closed with `\end{{...}}` matching their `\begin{{...}}`. The code must be compilable.

**## 5. LaTeX Structure Best Practices (필수 - 컴파일 오류 방지):**

**모든 \begin{{...}}에는 반드시 대응하는 \end{{...}} 태그가 있어야 합니다!**

**✓ 올바른 예시 - 2단 레이아웃:**
```latex
\begin{{frame}}
  \frametitle{{슬라이드 제목}}
  \begin{{columns}}
    \begin{{column}}{{0.5\textwidth}}
      왼쪽 컬럼 내용
      \begin{{itemize}}
        \item 항목 1
        \item 항목 2
      \end{{itemize}}
    \end{{column}}
    \begin{{column}}{{0.5\textwidth}}
      \includegraphics[width=0.9\columnwidth]{{extracted/figure_01.pdf}}
    \end{{column}}
  \end{{columns}}
\end{{frame}}
```

**✗ 잘못된 예시 - 피해야 할 일반적 오류:**
```latex
% 오류 1: \end{{column}} 누락
\begin{{frame}}
  \begin{{columns}}
    \begin{{column}}{{0.5\textwidth}}
      내용
    % 누락: 다음 begin 전에 \end{{column}}이 필요!
    \begin{{column}}{{0.5\textwidth}}
      더 많은 내용
    \end{{column}}
  \end{{columns}}
\end{{frame}}

% 오류 2: \end{{frame}} 누락
\begin{{frame}}
  \frametitle{{제목}}
  내용
% 누락: \end{{frame}}
```

**⚠️ 컴파일을 망가뜨리는 일반적 오류:**
- \end{{frame}} 누락 - 모든 프레임은 반드시 닫아야 함
- \end{{column}} 누락 - 다음 컬럼 시작 전에 닫아야 함
- \end{{columns}} 누락 - columns 환경은 반드시 닫아야 함
- \end{{itemize}} 또는 \end{{enumerate}} 누락
- 이전 컬럼을 닫지 않고 중복된 \begin{{column}}

**✓ 생성 전 체크리스트 (출력 전 필수 확인):**
1. 모든 \begin{{frame}} 개수 = \end{{frame}} 개수?
2. 모든 \begin{{columns}} 개수 = \end{{columns}} 개수?
3. 모든 \begin{{column}} 개수 = \end{{column}} 개수?
4. 모든 \begin{{itemize}} 개수 = \end{{itemize}} 개수?
5. 각 \begin{{column}}마다 다음 \begin{{column}} 전에 \end{{column}}이 있는가?
6. 모든 프레임이 새 프레임 시작 전에 \end{{frame}}으로 닫혔는가?

**개수가 맞지 않으면 출력 전에 수정하세요!**

**## Task**
Generate the complete LaTeX code for the presentation based on the provided research paper PDF and the instructions above.